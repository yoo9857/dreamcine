/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: Next.js 가 관리하는 타입 진입점이다. 이 파일을 직접 편집하지 않는다.
// `next build` 는 여기에 `.next/types/routes.d.ts` 참조를 덧붙이는데,
// 그 대상은 빌드 산출물이라 커밋하지 않는다.
