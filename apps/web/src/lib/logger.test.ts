import { describe, it } from 'vitest'

describe('logger', () => {
  it.todo('JSON 한 줄로 출력한다')
  it.todo('password / passwordHash 를 [REDACTED] 로 가린다')
  it.todo('token / secret / signedUrl 을 [REDACTED] 로 가린다')
  it.todo('요청 쿠키와 Authorization 헤더를 [REDACTED] 로 가린다')
  it.todo('LOG_LEVEL 보다 낮은 레벨은 출력하지 않는다')
})
