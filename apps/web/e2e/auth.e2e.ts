import { test } from '@playwright/test'

// US-01: 가입 → 인증메일 토큰 → 이메일 인증 → 로그인
test.fixme('가입한 사용자가 이메일을 인증하고 로그인한다', () => {
  // T03/S3 에서 구현한다.
})

test.fixme('script-src 에 unsafe-inline / unsafe-eval 이 없다', () => {
  // T03/S3 에서 구현한다.
})
