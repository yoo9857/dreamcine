node_modules
docs
.next
.turbo
coverage
dist
playwright-report
test-results
pnpm-lock.yaml

# next build 가 자기 형식으로 다시 쓰는 파일
apps/web/tsconfig.json

# predev/prebuild가 maplibre-gl 패키지에서 그대로 복사하는 벤더 워커
apps/web/public/maplibre

# 토큰에서 생성되는 파일 (contract:tokens 가 정본을 보장)
packages/ui/src/tokens/theme.css

# 로컬 작업용 스크래치
/tmp
